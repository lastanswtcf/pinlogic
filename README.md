# pinlogic v2

scrapes pinterest boards and saves all pin images as a json file, uses a real browser (brave or chrome) to bypass pinterest blocking, scrolls through the entire board automatically, filters out ads and promoted pins, and downloads images at original quality ;d
